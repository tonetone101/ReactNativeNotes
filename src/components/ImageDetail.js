import React from "react";
import { Text, StyleSheet, Button, View, TouchableOpacity } from "react-native";

const ImageDetail = () => {
  return <Text>Show Image of Something</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 50,
  },
});

export default ImageDetail;
